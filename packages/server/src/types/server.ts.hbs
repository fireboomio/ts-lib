/** Auto generated by Fireboom server */
{{#each serverObjectFieldArray}}
export type {{upperFirst (joinString '_' documentPath)}} = {{#if fields}}{
  {{#each fields}}
  {{name}}{{#unless required}}?{{/unless}}: {{> field_type this=this upFirst=true }}
  {{/each}}
}{{else}}
{{#if additional}}{{#equal typeName 'object'}}Record<string, {{/equal}}{{#with additional}}{{> field_type this=this }}{{/with}}{{#equal typeName 'object'}}>{{else}}[]{{/equal}}{{else~}}
any{{/if~}}
{{/if}}

{{/each}}

{{#each serverEnumFieldArray}}
export enum {{upperFirst name}} {
  {{#each values}}
  {{upperFirst (replaceSpecial (getOrDefault valueAliasMap this this) '_')}} = {{#equal valueType 'string'}}'{{/equal}}{{this}}{{#equal valueType 'string'}}'{{/equal}}{{#unless @last}},{{/unless}}
  {{/each}}
}
{{/each}}
