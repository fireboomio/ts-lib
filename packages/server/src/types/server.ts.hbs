/** Auto generated by Fireboom server */
{{#each serverObjectFieldArray}}
export type {{upperFirst (joinString '_' documentPath)}} = {{#if fields}}{
  {{#each fields}}
  {{name}}{{#unless required}}?{{/unless}}: {{> field_type this=this upFirst=true }}
  {{/each}}
}{{/if}}
{{~#if additional}}{{#equal typeName 'object'}}Record<string, {{/equal}}{{#with additional}}{{> field_type this=this }}{{/with}}{{#equal typeName 'object'}}>{{else}}[]{{/equal}}{{/if}}
{{/each}}

{{#each serverEnumFieldArray}}
export enum {{upperFirst name}} {
  {{#each values}}
  {{upperFirst (getOrDefault aliasKeys this this)}} = {{#equal valueType 'string'}}'{{/equal}}{{this}}{{#equal valueType 'string'}}'{{/equal}}{{#unless @last}},{{/unless}}
  {{/each}}
}
{{/each}}
